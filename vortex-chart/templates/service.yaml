apiVersion: v1
kind: Service
metadata:
  name: {{ include "vortex-chart.fullname" . }}
  labels:
    {{- include "vortex-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vortex-chart.selectorLabels" . | nindent 4 }}
